stages:
    - test
    - build

test:
    stage: test
    script:
        - "./bin/test"
    tags:
        - android
        - java

build:
    stage: build
    script:
        - "./bin/build --no-sign"
    tags:
        - android
        - java

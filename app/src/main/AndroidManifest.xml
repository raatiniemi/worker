<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.raatiniemi.worker"
    >

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_scheme"
        android:label="@string/name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/Theme"
        android:supportsRtl="true"
        >

        <activity
            android:name=".presentation.projects.view.MainActivity"
            >

            <intent-filter>

                <action
                    android:name="android.intent.action.MAIN"
                    />

                <category
                    android:name="android.intent.category.LAUNCHER"
                    />

            </intent-filter>

        </activity>

        <activity
            android:name=".presentation.project.view.ProjectActivity"
            android:parentActivityName=".presentation.projects.view.MainActivity"
            />

        <activity
            android:name=".presentation.view.activity.SettingsActivity"
            android:parentActivityName=".presentation.projects.view.MainActivity"
            />

        <provider
            android:label="@string/name"
            android:name=".data.WorkerProvider"
            android:authorities="me.raatiniemi.worker"
            android:syncable="false"
            android:exported="false"
            />

        <service
            android:name=".data.service.DataIntentService"
            android:exported="false"
            />

        <service
            android:name=".data.service.PauseService"
            android:exported="false"
            />

        <service
            android:name=".data.service.ClockOutService"
            android:exported="false"
            />

        <service
            android:name=".data.service.ResumeService"
            android:exported="false"
            />

    </application>

</manifest>
